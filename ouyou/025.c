/*
桁ごとの数字の和を求める
問題文：
ユーザーから整数を1つ入力し、その数の桁ごとの数字の合計を計算して表示するプログラムを作成しなさい。

例：

入力：1234
出力：桁ごとの合計は10です（1 + 2 + 3 + 4 = 10）
ヒント：

while文を使用しても良いですが、今回はfor文を使って計算してください。
数字を分割するために、割り算（/）と剰余（%）演算子を使います。
*/
#include <stdio.h>

int main(void)
{
    int num;

    printf("整数を入力してください：");
    scanf("%d", &num);

    int sum = 0; // 桁ごとの数字の合計

    // 数字の桁を計算するfor文を記述

    printf("桁ごとの数字の合計は%dです。\n", sum);

    return 0;
}
